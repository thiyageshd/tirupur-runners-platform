# DEPRECATED — replaced by .github/workflows/deploy.yml (Render deployment)
# Kept as a valid no-op workflow to avoid deletion conflicts.
name: "[disabled] Deploy Frontend to Cloudflare Pages"
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Deployments now use deploy.yml via Render."
