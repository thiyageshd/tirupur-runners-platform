# DEPRECATED — replaced by .github/workflows/deploy.yml (Render deployment)
# This file is intentionally left empty / disabled.
name: "[disabled] Deploy Frontend to Cloudflare Pages"
on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Disabled — use deploy.yml for Render instead"
        required: false
jobs: {}
