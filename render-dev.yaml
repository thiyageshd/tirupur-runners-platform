## Render Blueprint — DEVELOPMENT / STAGING
## Linked to: dev branch

services:
  # ─── Backend: FastAPI (Dev) ────────────────────────────────────────────────
  - type: web
    name: tirupur-runners-api-dev
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: DATABASE_URL
        value: "postgresql+asyncpg://tirupur_runners_user:Havsfdt0oZp0S7chCRJEQSAgplBFntPI@dpg-d74bn8oule4c73evi4e0-a/tirupur_runners_dev"
      - key: SECRET_KEY
        generateValue: true
      - key: RAZORPAY_KEY_ID
        value: rzp_test_SWZaFHz5JV0xYQ
      - key: RAZORPAY_KEY_SECRET
        value: kQDQE8uOUek1C21UPlr77tWh
      - key: RAZORPAY_WEBHOOK_SECRET
        value: kQDQE8uOUek1C21UPlr77tWh
      - key: MEMBERSHIP_AMOUNT_PAISE
        value: "200000"
      - key: DEBUG
        value: "True"
      - key: ADMIN_EMAIL
        value: "admin@tirupurrunners.com"
      - key: SENDGRID_API_KEY
        sync: false
      - key: FROM_EMAIL
        value: "noreply@tirupurrunners.com"
      - key: FRONTEND_URL
        value: "https://tirupur-runners-web-dev.onrender.com"

  # ─── Frontend: React + Vite (Dev Static Site) ─────────────────────────────
  - type: web
    name: tirupur-runners-web-dev
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: "https://tirupur-runners-api-dev.onrender.com/api/v1"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
