# DEPRECATED — this file has been superseded by /render.yaml (repo root).
# Render Blueprints read render.yaml from the repo root.
# See: /render.yaml  (production)
#      /render-dev.yaml  (development / staging)
